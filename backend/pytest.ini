[pytest]
testpaths = tests
pythonpath = .
markers =
    unit: fast, deterministic, no ML model or DB required
    integration: exercises the real IndicTrans2 translation model (loaded once per test session)
    e2e: end-to-end behavioral checks across the full translation pipeline
